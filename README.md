# Easy way to switch aws profiles

# Motivation

I was tired of always write the command `aws configure list-profiles` and then `export AWS_PROFILE=xxxx` so I wrote this small script to make it easier.

